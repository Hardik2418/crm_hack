version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install

  build:
    commands:
      - echo Starting app check
      - node app.js & sleep 5
      - echo App ran successfully

artifacts:
  files:
    - '**/*'
